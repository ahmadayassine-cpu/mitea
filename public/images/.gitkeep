Real photography goes here.

What is real so far: the logo and shop interior (from the Square site), and the
ten photos from the owner's menu document "menu para pantallas.pdf" — one per
mochi donut box, and one per drink series, shared by every item in that series
the way the document does it. Chizu has no photo in the document, so those items
still render a branded placeholder. To swap in a real photo:

1. Drop the file in this folder, e.g. `chizu-strawberry-green.jpg`.
2. Set the matching `image` field to `/images/chizu-strawberry-green.jpg`:
   - menu items      → lib/catalog/items.ts
   - category tiles  → lib/catalog/categories.ts
   - home page art   → components/home/sections.tsx (the `MediaSlot` calls)

No component changes are needed; `components/ui/media-slot.tsx` switches to
`next/image` as soon as `src` is set. Item cards are 4:3, category tiles 3:2,
gallery tiles 1:1, hero 4:3.

TODO(owner): `single-mochi-donut.jpg` came out of the menu document carrying a
SimplyHomeCooked.com watermark — it is a stock photo, not the shop's own.
Replace it before launch.
