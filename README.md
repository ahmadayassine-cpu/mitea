# mitea

This project is ready to be tracked with Git and published to GitHub.

## Getting started

1. Add your application code here.
2. Commit your changes.
3. Create a remote repository on GitHub and push.

## GitHub setup

```bash
git remote add origin https://github.com/<your-username>/mitea.git
git branch -M main
git push -u origin main
```
